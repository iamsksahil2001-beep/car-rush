// Fill this in with your own Firebase project's web app config:
// Firebase Console -> Project settings -> General -> "Your apps" -> Web app.
//
// These values are meant to be public/embedded in client-side code — Firebase
// access control comes from the Realtime Database security rules, not from
// hiding this config. Safe to commit to a public GitHub repo.
//
// Until you fill in a real apiKey below, the game automatically falls back to
// a per-browser leaderboard (localStorage) so everything still works.
const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT_ID.firebaseapp.com',
  databaseURL: 'https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT_ID.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
};
